import 'package:flutter/material.dart';

class AllocationsView extends StatelessWidget {
  const AllocationsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Center(child: Text('Allocations Page')),
    );
  }
}
